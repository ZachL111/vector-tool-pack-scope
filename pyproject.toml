[project]
name = "vector-tool-pack-scope"
version = "0.1.0"
description = "Package a Python local lab for pack analysis with round-trip fixtures, lossless normalization checks, and documented operating limits."
requires-python = ">=3.11"
